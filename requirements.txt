
lerobot[dataset,smolvla]==0.6.0

peft>=0.11.0

transformers>=4.45.0
accelerate

# Websocket policy serving (OpenPI-compatible client/server)
websockets>=12.0
msgpack>=1.0

# Only needed if you go GPU (recommended: RTX 3090/4090, 24GB)
# torch is installed automatically as a lerobot dependency
